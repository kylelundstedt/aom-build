"""AOM Phase A adapters.

Fixture-driven implementations of the aom-build contracts. Nothing in this
package talks to a live Shelley, Hermes, Entire, or Product system; providers
are injected so the same logic can later bind to real environments after
authorization.
"""
